export const selectProducts = (state) => state.library.products;

export const selectCategories = (state) => state.library.categories;

export const selectCurrentCategory = (state) => state.library.currentCategory;
